# CMPT-354-Project
A database system for a library that manages items, patrons, borrowing, fines, events, rooms, and personnel. The project includes an ER model, a normalized schema, a populated SQLite database, and a Python application that supports core library operations.
